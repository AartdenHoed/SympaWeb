<div class="controller">
    @model ConfigMan.ViewModels.ComputerVM

    @{
        ViewBag.Title = "Computer Delete";
    }

    <h2>Delete</h2>

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Computer</h4>
        <hr />
        <dl class="sy-dl">
            <dt class="sy-dt">
                @Html.DisplayNameFor(model => model.ComputerName)
            </dt>

            <dd class="sy-dd">
                @Html.DisplayFor(model => model.ComputerName)
            </dd>

            <dt class="sy-dt">
                @Html.DisplayNameFor(model => model.ComputerPurchaseDate)
            </dt>

            <dd class="sy-dd">
                @Html.DisplayFor(model => model.ComputerPurchaseDate) &nbsp;
            </dd>

            <dt class="sy-dt">
                @Html.DisplayNameFor(model => model.OS)
            </dt>

            <dd class="sy-dd">
                @Html.DisplayFor(model => model.OS) &nbsp;
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Verwijder" class="sy-button" /> 
               
            </div>
        }
        <div>
            @Html.ActionLink("Terug naar lijst", "Index","", new { @class = "sy-a" } )
        </div>
    </div>
</div>
